info:
  name: wizard3
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/wizards
  body:
    type: json
    data: |-
      {
        "name": "Luna Lovegood",
        "house": "Ravenclaw",
        "patronus": "Liebre",
        "isDeatheater": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
