info:
  name: wizar4
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/wizards
  body:
    type: json
    data: |-
      {
        "name": "Severus Snape",
        "house": "Slytherin",
        "patronus": "Cierva",
        "isDeatheater": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
